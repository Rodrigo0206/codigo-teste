package lambda;

import java.util.ArrayList;
import java.util.List;

public class Main {
    public static void main(String[] args) {

        (argumento) -> expression
                (argumento, argumento) -> expression
                () -> {bloco}

        List<Cliente> clientes = new ArrayList<>();
        clientes.add(new Cliente(rodrigo))
    }
}
